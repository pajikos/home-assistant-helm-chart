# CI test configuration for code-server addon with authentication enabled
persistence:
  enabled: true
  size: 1Gi

addons:
  codeserver:
    enabled: true
    auth:
      enabled: true
      password: "test-password-123"
    service:
      type: ClusterIP
    ingress:
      enabled: true
      hosts:
        - host: code-server-test.local
          paths:
            - path: /
              pathType: ImplementationSpecific
